SPELLING BEE SAVE GAMES TO SERVER

if the word gets added, write the localStorage to a json file
--- GameGUI.checkWord()

save_20260423.json

when loading game, check to see if there's a save file before checking localStorage. if there is, load that to localStorage. then load game as usual.
--- LoadGame.loadGame()


JAVASCRIPT
X add sendData() to new GameSave() class, rename it
writeSave()
X [GameGUI.checkWord()] if word gets added, call writeSave()
X [LoadGame.loadGame()] when loading game, check to see if there's a save file before
checking localStorage. if there is, load the save game to localStorage.
else load game as usual
X use localStorage.clear() to clear before writing the save to localStorage

s = new GameSave(this.gamenum);
const hasSaveFile = await s.hasSave();

GameSave.hasSave() {
    fetchData()
}

[Controller.py]
has_save():

[api.py]
X /save/check/<game>
X returns boolean, true if save file exists, false if not

- remove the saves folder from getAllGames

SaveGame.isSave()
- returns true or false if there's a save game
- write new function in Controller.py and api endpoint to check if there's a save

SaveGame.writeSave()
x SaveGame.checkSaves()
gameStorage.writeSave()

PYTHON - api.py
X /save/[gamenum]

PYTHON - Controller.py
X SaveGame()
X write_save(data:dict)
X open/create json file, write data